language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-parse-gulp-less' ]; then cd .. && eval "mv $currentfolder generator-parse-gulp-less" && cd generator-parse-gulp-less; fi
